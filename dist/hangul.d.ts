export declare function commandParts(char: string): string | null;
export declare function decomposableSpelling(text: string): boolean;
/** Preserve a compound-final slot so settings can disable its fixed expansion. */
export declare function commandSymbols(char: string): string | null;
