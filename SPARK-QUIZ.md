# GPT-5.3-Codex-Spark와 자모 문제 풀이

새 실행기로 계산한 결과와 Spark가 도구 없이 추론한 출력을 비교했다. 풀이 전에 문제에 필요한 언어 규칙을 제공했다. 이 기록은 10개 예제의 작은 실험이며 일반적인 모델 성능이나 입문자의 학습 효과를 입증하지 않는다.

| 문제 | 내용 | 실행 결과 | Spark 답변 |
|---|---|---|---|
| 1 | continue와 합계 | `8` | 일치 |
| 2 | 반복별 독립 참조 | `1 20 3` | 일치 |
| 3 | 매크로 재등록 | `5`, `6` | 일치 |
| 4 | 꺼내 쓴 메서드와 인스턴스 필드 | `3 3 11 11` | 일치 |
| 5 | 디버거 재개·문자열 길이·자료형 | `0`, `2 문자 문자열` | `0`, `2 char string` — 형 이름의 정확한 출력 표기를 출제자가 누락해 채점 제외 |
| 6 | 삽입·삭제 뒤 참조 | `20`, `끝난 자리` | 일치 |
| 7 | 얕은 목록 복사 | `[[9], [2]] [[9], [8]]` | 일치 |
| 8 | 실행되지 않는 분기 안의 매크로 등록 | `A` | 이름 미정의 오류라고 답해 불일치 |
| 9 | 객체 필드의 엄격한 참조 | `7 2` | 일치 |
| 10 | 큰 정수와 내림 나눗셈 | `9007199254740994 ㅗ`, `-3 2` | 일치 |

**10문제 중 출력 완전 일치 8개, 출제 조건 누락으로 제외 1개, 오답 1개**였다. 충분한 조건을 제공한 9문제 기준으로는 8개가 맞았다. 전체 코드와 실행 결과는 [실행 기록](verification/spark-execution-key.json)에 있다.

## 드러난 설명 지점

8번에는 “매크로는 실행 전에 소스 순서대로 등록하므로 실행되지 않는 분기 안에서도 보인다”는 규칙을 명시했지만, Spark는 이를 실행 단계의 변수 선언처럼 해석했다.

```text
ㅁ ㅗ ㄱ
    ㅊ문구 = "A"
ㅋ
ㅂ(문구)
```

정답은 `A`다. 사용자 자료형의 괄호형 선언은 실행 단계이고, 괄호 없는 매크로 등록은 소스를 읽는 단계라는 차이를 README에 추가했다.

규칙을 교정한 뒤 아래 문제를 한 번 더 냈다.

```text
ㅁ ㅗ ㄱ
    ㅊ문구 = "A"
    ㅊ상자()
        ㅒ 값 = 1
    ㅋ
ㅋ
ㅂ(문구)
ㅑ
    상자()
ㅏ
    ㅂ("타입 없음")
ㅋ
```

Spark는 `A`와 `타입 없음`을 순서대로 출력한다고 답하며 두 정의의 처리 단계를 구분했다. 이 교정 문제는 최초 10문제의 점수에 더하지 않았다.
