# TypeScript 이식 기록

공개 게시 후 추가 기록: https://github.com/slyni7/JaMo 와 https://slyni7.github.io/JaMo/ 에 게시했다. 공개 배포 파일 9/9개를 로컬 파일과 대조하고 내장 브라우저 실행·오류 처리 4/4개를 확인했다(`verification/pages.json`). 아래의 미게시·미검증 표기는 각 이식 단계 당시의 기록이다.

최신 모아쓰기 추가: 사용자 지시인 대응단어 → 선언 → 바닐라 순서를 구현했다. `src/hangul.ts`와 frontend.ts의 표기 해석 단계가 완성형/부분 조합/현대 결합 자모/채움문자를 분해한다. 소스 선언과 기존 Env의 이름 및 문자열·문자·주석을 보호한다. `wordAliases` 호스트 맵은 이름보다 먼저 적용하며 기본 언어팩 UI는 추가하지 않았다. 소스에서 이름 철자를 수집해도 실행 시 변수 수명과 지역 범위는 바뀌지 않는다. 선언되지 않은 순한글 표기는 이제 명령으로 분해하므로 해당 종전 구문 시험에는 선언 정보를 추가했다.

최신 검증: 152/152 자동 시험(프런트엔드60, 런타임53, 모아쓰기/우선순위/인코딩17, CLI8, README7+기본예제7), headless Edge 10/10. 현대 음절 11,172개 NFD 대조와 CP949 왕복을 포함한다. CP949 확장 바이트를 잘못 해석하던 기존 TextDecoder 경로를 iconv-lite 및 엄격 바이트 검사로 수정했다. 기존 Python은 그대로 보존하며 아래의 132/93/11 등은 이전 판 기록이다.

사용자가 2026-09-13에 TypeScript로 새로 작성하도록 명시했다. Python 구현은 검증 기준으로 보존하고 src/frontend.ts와 src/runtime.ts로 실행 의미를 이식한다. HTML/CSS 화면과 TypeScript 웹 실행 연결부는 GitHub Pages에서 사용할 수 있도록 추가한다. 실제 GitHub 저장소 게시 요청이나 대상은 아직 없다.

보존/이식: 기존 39개 기능, 파서 구조와 위치 정보, 임의 정밀도 정수, 유한 실수, 문자/문자열 구분, 새 반복 범위·셀, 관리 참조, 예외와 제어 흐름, 모듈 경로·캐시, 디버거 정지·재개. Python eval/exec나 JS eval로 사용자 코드를 실행하지 않는다.

사용자 정정: 영문 if 뒤에는 공백을 요구하지만 자모 ㅁ 뒤에는 요구하지 않는다. 이전에 ㅁ 공백 필수를 사용자 확정으로 기록한 것은 에이전트의 오독이었다. ㅁ_조건, ㅁ(조건), ㅁㅖ를 허용하도록 수정했다. _조건은 밑줄을 포함한 변수 이름이다. 기본 영문별칭은 아직 미구현이며, 직접 ㅊif=ㅁ으로 등록한 단어 별칭의 공백 조건은 유지한다.

사용자 요구: ㅊ은 custom/추상화이며 본문에 이 언어의 구문을 허용하고 class/struct처럼도 사용한다. 다음 세부 문법은 구현자가 구체화한 선택이다. 괄호 없는 ㅊ챣 블록은 소스의 토큰 매크로로 등록해 이후 챣에서 펼친다. ㅊ이름 = 토큰열은 연산자 등 구문 조각도 받는다. ㅊ사람(이름값) 블록은 필드와 메서드를 가진 사용자 자료형으로 정의해 사람("철수")로 독립 인스턴스를 만든다.

구현 선택: 매크로는 소스의 순서에 따라 등록하며 런타임 조건과 별도로 처리한다. 정확한 이름 토큰을 바꾸는 비위생적 매크로이므로 같은 이름 변수도 바뀐다. 매크로와 자료형의 동일 이름 등록은 진단하고, 같은 매크로의 재등록은 이후 사용부터 갱신한다. 자료형 생성 매개변수는 공개 필드와 분리하며, 생성 본문의 ㅒ가 필드를 만든다. 자신은 인스턴스 참조이고 메서드는 필드 범위를 유지한다. 생성자의 빈 반환은 생성을 종료하며 값 반환은 객체를 대체하지 않고 오류가 된다. 상속·접근 제한자·제네릭·구조체 메모리 배치는 구현하지 않았다.

40개 자모의 영문·한글 별칭 표는 제안 상태이다. 아직 채택되지 않은 영문·한글 별칭을 전역 예약어로 만들어 기존 변수명을 바꾸지 않는다. + - * /는 기존 연산자로 유지한다. 피드백에서 확인된 1차 목표는 사용자 자신의 스마트폰 입력 편의이며, 입문자의 단어 입력은 선택형 언어팩으로 제안한다. `ㅅ 이름` 형 선언과 임의 자모 변수 이름은 검토 중이며 현재 실행기의 선언은 ㅒ이다.

브라우저 연결: 실행을 Worker로 분리하고 입력·디버거 이벤트를 메시지로 연결한다. 메모리의 파일들을 가져오기/읽기/쓰기에 사용하며, UI에서 텍스트 파일을 열거나 내려받는다. 저장 여부가 드러나게 안내한다. CLI는 Node의 실제 파일 입출력을 사용한다.

반례: JS number로 9007199254740993을 읽으면 정수 값이 손상된다. 따라서 정수는 bigint를 쓴다. UTF-16 length는 이모지 문자를 두 칸으로 세므로 언어 문자열 길이·문자 위치는 코드포인트 기준을 보존한다. 비동기 입력을 기다리는 중에도 중단할 수 있어야 하고, 디버거 중단은 언어의 예외 처리로 무효화되지 않아야 한다.

구문 분석, TS 타입 검사/컴파일, Node 실행, 기존 Python과의 차등 검증, 실제 브라우저 실행은 각각 별도로 검증한다. 공백 규칙 정정 후 타입 검사·빌드와 자동 시험 132/132개를 통과했다. 분모는 프런트엔드 60개, 런타임 53개, CLI 7개, README·기본 예제 12개다. 실제 Edge에서 새 경계 규칙 3/3개도 확인했다(verification/if-boundary-browser.json). 기존 별도 Edge 11/11개 및 Python·TS 차등 93/93개는 정정 전 판의 실행 기록이다. 실제 휴대전화 입력과 공개 GitHub Pages 배포는 검증하지 않았다.
