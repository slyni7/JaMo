# 작업 인계 — 2026-09-13

## 최신 변경 — 개인 설정

아디나가 요청한 다섯 설정을 구현했다. 겹자모 입력 보조 기본 300ms, 종이/밤/고대비·강조색, 소리 켜기/끄기·음량, 40+11 표기의 실행 허용/차단, 한글·영문 대응단어 편집이다. 대응 편집은 **단어 별칭**이며 자모 기능 재배정은 제외한다. `ㅛ`의 라벨·전달 자리와 초성/종성 명령의 기존 역할을 유지한다.

`src/settings.ts`는 모델·검증·테마, `settings-panel.ts`는 실제 설정창, `input-assist.ts`는 편집기/화면 키보드의 11쌍 결합, `feedback.ts`는 선택형 Web Audio 소리를 담당한다. `app.ts`에서 실행마다 별칭·차단 목록을 Worker로 보내고 `runtime.ts`는 가져온 모듈에도 같은 구문 분석 설정을 사용한다. `frontend.ts`·`hangul.ts`는 분해 전 기호의 출처를 보존해 겹자모·모아쓰기·별칭의 차단 우회를 막는다. 선언한 이름·문자열·주석은 보존한다. 꺼진 명령은 실행 전에 소스 전체에서 검사하며, 선택한 설정은 다음 실행부터 적용한다.

설정 영구 저장은 연결하지 않았다. 사용자는 다섯 항목 구현 후 쿠키 사용 여부를 검토하라고 했으며, 서버에 불필요한 설정이므로 후속 저장은 localStorage를 제안한다. 기존 열린 소스 파일의 localStorage 자동 저장은 그대로다. 현재 개인 설정은 새로고침하면 기본값으로 돌아간다.

타입 검사·빌드와 자동 시험 240/240개(최상위 234+하위 6)가 통과했다. `verification/settings-tests.tap`과 `settings-browser.json`에 범위를 기록한다. 실제 브라우저 10개 경로를 확인했고, 물리 Android/iPhone 입력기·실제 스피커 청취는 미검증이다. 기존 자동 시험 197개를 유지하고 입력 18·설정 정책 18·사운드 7개를 추가했다. README도 구현 상태와 저장 미연결을 반영했다.

아래는 이전 단계의 이력이다. 이전의 ‘설정 목업·별칭 화면 미구현’ 및 시험 수는 해당 단계의 기록이며 위 최신 상태를 우선한다.

## 이전 단계 기록

사용자가 프로젝트 이름을 `JaMo`로 정하고 Git 첫 커밋과 Codex 공동 작성자 표기를 지시했다. 기존 `hunmin-ts/` 경로·패키지명·실행 의미는 유지하며 README와 웹의 이름을 JaMo로 반영했다. README에 원본 대화에서 확인한 최초 구상·아침 설계 시각 및 설계/구현 협업 역할을 기록했다. 공식 Codex 공동 작성자 trailer는 `Co-authored-by: Codex <noreply@openai.com>`이다. 기존 Python 프로젝트는 별도 위치에 보존한다.

## 목표와 완료 범위

사용자가 명시한 TypeScript 이식, 브라우저와 Node CLI 실행, `ㅊ`의 토큰 매크로 및 class/struct 형태 사용자 자료형, GitHub README용 제안서, GPT-5.3-Codex-Spark 문제 풀이를 수행했다. 기존 Python 산출물은 변경하지 않았다. 공개 저장소는 https://github.com/slyni7/JaMo 이며 origin은 https://github.com/slyni7/JaMo.git 이다. 웹 실행기는 https://slyni7.github.io/JaMo/ 에 게시했다.

새 산출물은 이 `hunmin-ts/` 프로젝트다. `src/frontend.ts`가 토큰화·매크로·파싱을, `src/runtime.ts`가 AST 실행과 관리 저장 칸을, `src/node-host.ts`·`src/cli.ts`가 로컬 파일 실행을 담당한다. `src/app.ts`·`src/worker.ts`·`src/protocol.ts`·`src/examples.ts`와 `public/`이 웹 화면을 구성한다. `dist/`와 `site/`는 빌드 결과다. README, PORTING, SPARK-QUIZ와 검증 기록을 함께 제공한다.

## 결정 유지

- 모아쓰기 추가: `몍`, `몌ㄱ`, `ㅁㅖㄱ`, 현대 결합 자모와 초성 없는 중성·종성+채움문자를 같은 명령열로 분해한다. 사용자 확정 우선순위는 대응단어 → 선언된 이름 → 기본 자모 해석이다. 등록 대응단어는 `ParseOptions.wordAliases` 및 `InterpreterOptions.wordAliases`로 지정하며 기본 팩·선택 화면은 추가하지 않았다.
- 이름 철자는 소스의 선언·매개변수·반복·함수·custom·import 자리와 기존 Env에서 수집한다. 이 수집은 선언을 실행하거나 값의 기존 지역 범위·수명을 바꾸지 않는다. 필드 접근의 이름도 보존한다. 남은 순한글은 명령으로 분해하므로 미정의 순한글 이름을 쓰던 종전 구문 시험에는 선언 정보를 명시했다. 미정의 호출 자체는 `_없는함수`처럼 비한글 표지로 표현한다. 문자열·문자·주석·선언된 철자는 전역 정규화하지 않는다.
- `src/hangul.ts`는 현대 음절 분해와 현대 결합 자모·채움문자 처리를 담당한다. 오류 열은 원래 소스의 코드 포인트 열이다. 겹받침은 음절/결합 종성 안에서 구성 자음으로 풀고 쌍자음·복합 모음은 기존 한 기호로 보존한다. 사용자가 모바일 키보드 구현이 아니라 Pages가 받은 문자의 수용을 지시하여 낱자 겹자모 `ㄳ ㄵ ㄶ ㄺ ㄻ ㄼ ㄽ ㄾ ㄿ ㅀ ㅄ` 11개에도 같은 분해를 적용했다. 새 명령 배정·호출 문법·입력기는 추가하지 않는다.

- 기존 39개 자모 의미를 이식하고 `ㅊ`를 배정했다. `ㅔ`와 `ㅝ`의 80% 잠정 채택 표시는 유지한다.
- 사용자 정정: 영문 `if` 뒤에는 공백을 요구하고 자모 `ㅁ` 뒤에는 요구하지 않는다. `ㅁ_조건`, `ㅁ(조건)`, `ㅁㅖ`, `ㅇㅁㅖ`를 허용한다. `_조건`의 밑줄은 이름의 일부다. 이전 `ㅁ` 공백 필수 기록은 에이전트의 오독이었다. `+ - * /`는 연산자다. 전역 NFKC 정규화를 하지 않는다.
- `ㅊ이름 = 토큰열`과 괄호 없는 블록 `ㅊ이름 … ㅋ`은 소스 순서대로 등록되는 토큰 매크로다. 사용 시 정확히 같은 이름을 펼치며 변수 이름도 예외가 아니다. 실행되지 않는 분기 안의 등록도 처리된다.
- 괄호형 `ㅊ이름(매개변수) … ㅋ`은 런타임 사용자 자료형이다. 생성 본문의 `ㅒ`가 필드를 만들고 `ㅎ` 메서드는 인스턴스의 필드 범위를 유지한다. 매개변수와 공개 필드를 구분한다. 생성자 빈 반환은 생성을 끝내고 값 반환은 오류다.
- `ㅓ·ㅕ`는 범용 관리 참조·읽기/쓰기, `ㅟ·ㅢ`는 엄격한 관리 참조·역참조다. 실제 임의 주소를 다루지 않는다. 반복마다 새 저장 칸을 만들고 탈출한 참조는 해당 칸을 유지한다.
- `ㅙ`에서 재개할 수 있으므로 조건 통과를 보장하지 않는다.

## 제안·미구현·제한

`ㅊ`의 새 기능 및 class/struct 역할은 사용자 요구이며, 괄호로 종류를 구별하고 이름 토큰을 치환하는 구체적 방식은 구현자의 선택이다. 사용자 명시 확정과 혼동하지 않는다. 현재 직접 `ㅊif=ㅁ`으로 등록한 단어 별칭은 공백을 요구하고, 기본 영문·한글 별칭은 추가하지 않았다.

영문·한글 별칭과 선택형 언어팩, `ㅅ 이름` 형 선언 및 임의 자모 변수 이름은 제안/검토 상태다. 현재 선언은 `ㅒ`, `ㅅ`은 정수 변환이다. 상속·접근 제한자·제네릭·C 구조체 메모리 배치, 슬라이스 대입, 문자열 보간, 타 언어 자동 번역은 없다. 현재 lexer가 허용하지 않는 한쪽 괄호만의 매크로 본문도 지원하지 않는다. 브라우저 파일 열기는 UTF-8, CLI는 UTF-8 및 명시적 CP949다. 이러한 제한을 구현 완료로 포장하지 않는다.

## 검증

- 낱자 겹자모 수용 후 타입 검사·빌드 및 자동 시험 159/159 통과. 프런트엔드 60, 런타임 53, 모아쓰기·우선순위·인코딩 24, 실제 CLI 8, README·기본 예제 14. 결과 `verification/cluster-tests.tap`. 추가 시험은 겹자모 11개 분해·인코딩·선언·custom 이름, 대응단어 우선순위, 리터럴 보존, 오류 열과 기존 문법 오류를 확인한다. 이후 항목은 이전 판 검증 기록이다.
- Node.js 24.19.0, TypeScript 7.0.2에서 `node tools/build.mjs` 타입 검사·빌드 통과.
- 모아쓰기 추가 후 `node --test --test-reporter=tap tests/*.test.mjs`: 152/152 통과. 프런트엔드 60, 런타임 53, 모아쓰기·우선순위·인코딩 17, 실제 CLI 8, README 전체 text 코드 7 및 기본 예제 7. 결과 `verification/tests.tap`.
- 현대 완성형 11,172개를 표준 NFD 분해와 대조했고 CP949 왕복도 11,172개를 확인했다. 기존 TextDecoder('euc-kr')의 UHC 확장 오류(`몍`의 91 75)를 찾아 node-host.ts를 iconv-lite CP949 및 엄격 바이트 검증으로 수정했다. Python3.12 독립 관찰도 91 75 = 몍을 확인했다.
- 모아쓰기 판의 별도 headless Edge 시험 10/10 통과. 새 예제, 완성/부분/현대 결합 자모, 이름·리터럴·custom 보존, custom을 통한 이름 선언, 호스트 대응단어 우선순위. `verification/hangul-browser.json`과 `hangul-browser.png`. 실제 입력기 자체는 미검증이다.
- 정정 후 별도 headless Edge에서 자모 붙여쓰기·영문 custom 별칭 공백 허용·공백 없는 영문 custom 별칭 거부의 3/3 경로를 확인했다. `verification/if-boundary-browser.json`.
- 정정 전 판에서 별도 Python·Node 프로세스로 차등 사례 93/93 통과. 큰 정수, Unicode, 반복 저장 칸, 참조, 제어 흐름, 파일·모듈·인코딩과 실패를 포함. `VERIFICATION-PARITY.json`은 각 엔진의 실제 관찰 결과다. 모든 문법 조합이나 경로의 완전한 커버리지를 뜻하지 않는다.
- 정정 전 판에서 별도 headless Edge 화면 경로 11/11 통과. 입력/EOF, 디버거 재개, Worker 중단, 파일 읽기/쓰기/모듈, 저장·내려받기·업로드, 390px 화면과 자모 40키를 포함. `verification/browser.json`과 PNG 3장. 실제 Android/iPhone과 공개 Pages는 미검증.
- GPT-5.3-Codex-Spark에 도구 없이 10문제 출제. 8개 완전 일치, 조건 누락 1개 채점 제외, 매크로 단계 문제 1개 오답. 교정 뒤 추가 1개 정답. `SPARK-QUIZ.md`와 `verification/spark-execution-key.json`에 기록.

## 배포와 후속 작업

2026-09-13 공개 완료: 사용자가 승인한 개인 경로·운영 메모 제거 후 첫 커밋은 `80b8b1b8a8d63df20c57d73236c66455ae05c521`이다. 작성자와 기존 Codex 공동 작성자는 유지했다. `gh-pages`의 `29b9cdf6f2776238424dfeb2c5799ccbcb4bca63`은 그 첫 커밋의 `site/`와 같은 트리를 루트에 게시한 배포 커밋이다. 소스 `main`과 배포 `gh-pages`는 별도이며 자동 동기화 설정은 없다. 공개 HTTP 파일 9/9개 일치 및 내장 브라우저 경로 4/4개는 `verification/pages.json`에 기록했다. 이후 설계·기능 변경은 아디나의 직접 지휘에 따른다.

소스 묶음은 상위 `hunmin-typescript.zip`, 정적 배포본은 상위 `hunmin-pages.zip`으로 만든다. Pages ZIP은 `site/` 내용을 루트에 둔다. 아카이브 CRC와 모든 구성 파일의 SHA-256 대조 결과는 상위 `typescript-bundle-verification.json`에 둔다. 소스 실행은 `pnpm install --frozen-lockfile`, `pnpm build`, `pnpm start`를 사용한다.

후속 변경은 기존 소스와 이 결정부터 확인한다. 새 언어팩이나 형 선언은 별도의 의미 결정이 필요하며 구현된 것으로 오해하지 않는다.
